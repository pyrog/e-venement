generator:
  class: sfDoctrineGenerator
  param:
    model_class:           Event
    theme:                 admin
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          event
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:
        meta_event_id: { label: Programmation }
        MetaEvent: { label: Programmation }
      list:
        batch_actions: []
        object_actions: []
        actions: []
        display: [=name, MetaEvent, EventCategory, _dates, _age_min]
        table_method: retrievePublicList
        sort: ['max_date, m.happens_at', '']
        title: Events list
      filter:
        display: [name, meta_event_id, event_category_id]
      form:    ~
      edit:    ~
      new:     ~

generator:
  class: sfDoctrineGenerator
  param:
    model_class:           Survey
    theme:                 jroller
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          survey
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:
        list_nb_answers_group: { credentials: [srv-answer] }
      list:
        display: [name, _list_nb_queries, _list_nb_answers_group]
        object_actions:
          _show: ~
          _edit: ~
          _delete:
            credentials: [srv-survey-del]
        actions:
          _new:
            credentials: [srv-survey-new]
        batch_actions:
          _delete:
            credentials: [srv-survey-del]
      filter:
        display: [name, weight, description]
      form:
        display:
          Général: [name, description, _show_queries, _show_extract_structure]
          Answers: [_show_answers, _show_extract_content]
          Used by: [ApplyTo]
          Statistics: [_show_stats]
      edit:
        title: Edit Survey %%name%%
        actions:
          _list: ~
          _delete:
            credentials: [srv-survey-del]
          _show: ~
          _add_query:
            credentials: [srv-query-new]
            action: addQuery
            ui-icon: plus
          _save:
            credentials: [srv-survey-edit]
      new:     ~
      show:
        display:
          Public form: [_show_form]
          Answers:     [_show_answers]

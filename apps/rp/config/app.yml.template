# You can find more information about this file on the symfony website:
# http://www.symfony-project.org/reference/1_4/en/11-App
# default values
sf_admin_theme_jroller_plugin:
  web_dir:      /sfAdminThemejRollerPlugin # specify your folder where to pick jquery and jquery UI.
  use_jquery:   true # default. use the packaged jquery/UI
  theme_dir:      /sfAdminThemejRollerPlugin # default. change the theme directory
  theme:        redmond # default.
  css_reset:    true # default. reset default css (from Blueprint CSS)
all:
  options:
    #remove_ticketting: true
    design: tdp
    #social_informations: true
    #no_event_archives: false
    #force_fields: [firstname, postalcode, city]
    #contact_force_one_phonenumber: true
    #labels_offset: 500
  #controls:
  #  enable: true
  cards:
    #enable: true
    types: [membership, subscription]
    expiration_delay: 1 year
    #expiration_date: 09-01
    #default_amount: 20
    extra: |
      Ciné Ma Passion - Le Méliès
      6 rue Bargouin - 64000 PAU
      www.lemelies.net - cinemapassion@lemelies.net
      05 59 27 60 52 - 08 92 68 68 87 (0,34&nbsp;€/min)
    date_extra: Cette inscription est valable 1 an (date de fin indiquée) et ouvre droit au tarif abonné.
    auto_close: true
  #carddav_sync:
  #  auth:
  #    userName: xxx
  #    password: PAssWOrd
  #    #proxy: xx
  #  contacts_url: https://mail.domain.tld/dav/zimbrauser/Contacts


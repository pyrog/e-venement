generator:
  class: sfDoctrineGenerator
  param:
    model_class:           Transaction
    theme:                 jroller
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          transaction
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:
        tickets_qty: { label: Tickets }
        delete_demands: { label: Del. }
      list:
        display: [_id, _contact, _organism, closed, _tickets_qty, _delete_demands, _debt, _type, User, updated_at]
      filter:
        display: [created_at, contact_id, organism_id, sf_guard_user_id]
      form:    ~
      edit:    ~
      new:     ~
